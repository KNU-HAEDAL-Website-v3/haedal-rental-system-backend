# 환경 변수 예시 파일
# 이 파일을 .env로 복사한 뒤 값을 채운다. .env는 저장소에 올리지 않는다.

# Django
DJANGO_SECRET_KEY=
DJANGO_SETTINGS_MODULE=config.settings.dev

# 운영 설정에서만 사용한다. 쉼표로 구분한다.
DJANGO_ALLOWED_HOSTS=

# PostgreSQL
DB_NAME=haedal_rental
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
